"""
In this prototype we are using define function with both 
parameter model and non parameter model

"""
def hello(to="World"):
    print("hello,",to)

name = input("What is your name?")

hello()

hello(name)
"""
Here the most Important thing is We must declare the function
before we use because python is interpreated language, It will
give error! 

"""
